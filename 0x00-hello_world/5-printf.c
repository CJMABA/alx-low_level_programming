#include <stdio.h>
/**
 * main - program that print a line using the printf function
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outome is a piece of art,\n")
		return (0);
}
